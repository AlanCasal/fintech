import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	cyberDots: {
		width: '100%',
		position: 'absolute',
		left: 0,
		zIndex: 2,
		elevation: 2,
	},
	top: {
		top: 0,
	},
	bottom: {
		bottom: 0,
	},
});
