import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	container: {
		position: 'absolute',
		width: '100%',
	},
	tabsContainer: {
		justifyContent: 'space-evenly',
		flex: 1,
		flexDirection: 'row',
	},
});
